services:
  - type: web
    name: my-php-app
    env: ubuntu
    buildCommand: |
      sudo apt-get update
      sudo apt-get install -y php libapache2-mod-php
    # startCommand: php -S localhost:8000
    plan: free